/**
 * Navigation shared by all three docs plugin instances.
 *
 * Keep these as links rather than doc references: each destination belongs to
 * a different Docusaurus plugin tree and retains its own version selector.
 */
const sharedSidebarShell = require('./src/config/sidebarShell.json');

module.exports = {sharedSidebarShell};
