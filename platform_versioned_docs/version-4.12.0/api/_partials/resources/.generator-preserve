# Paths listed here are backed up before os.RemoveAll and restored afterward,
# so they survive generator runs even when the corresponding API field no longer
# exists in the current api/v4 version.
#
# Add an entry whenever the generator deletes a partial that frozen versioned
# docs still import via @site/platform/api/_partials/... .

# External config field — removed in api/v4 v4.8.1, but imported by frozen
# vCluster versioned docs 0.28, 0.29, 0.30.
config/external.mdx
config/external/
