```bash
vcluster platform delete namespace [namespace-name]
```

:::tip Kube-Context
Deleting namespaces with the vCluster CLI has the advantage that the vCluster CLI will also delete the kube-context for this space from your local kube-config file to keep everything cleaned up.
:::
