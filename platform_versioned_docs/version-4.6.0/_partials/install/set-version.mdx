Set the `VERSION` variable to the platform version you want to upgrade to or set it to the current version using:

```bash title="Set VERSION variable"
CHART=$(kubectl get service loft -n vcluster-platform -o jsonpath={.metadata.labels.chart})
VERSION=${CHART:5}
```
