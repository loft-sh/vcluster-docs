---
sidebar_label: Restore a Platform External Database
sidebar_position: 4
title: Restore a Platform Deployment with an External Database
description: Procedure to restore a vCluster Platform with an external database from a snapshot.
---

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";
import VersionBadge from '@site/src/components/VersionBadge';
import InterpolatedCodeBlock from "@site/src/components/InterpolatedCodeBlock";
import PageVariables from "@site/src/components/PageVariables";

<VersionBadge platformVersion="v4.8.0"/>

<PageVariables
  PLATFORM_DOMAIN="platform.example.com"
  ACCOUNT_ID="123456789012"
  AWS_REGION="us-east-1"
  DB_VPC_ID="vpc-xxxxxxxxx"
  DB_SG_ID="sg-xxxxxxxxx"
  CLUSTER_CONTEXT="arn:aws:eks:us-east-1:123456789012:cluster/platform-ha"
  CURRENT_DB_INSTANCE_ID="mariadb-ha-platform"
  NEW_DB_INSTANCE_ID="mariadb-ha-platform-restored"
  SNAPSHOT_NAME="kine-backup-YYYY-MM-DD"
/>

This procedure describes how to restore the Kine database from an RDS snapshot to
a new instance and update the platform to use the restored database. Use this
procedure for disaster recovery, database migration (for example, enabling IAM
authentication), or point-in-time recovery.

:::warning
The platform deployment must be scaled down before switching the data source to
prevent split-brain writes to the old and new databases.
:::

## Step 1 - Create a snapshot of the current database

Skip this step if you already have a snapshot to restore from.

<InterpolatedCodeBlock
  code={
`aws rds create-db-snapshot \\
  --db-instance-identifier [[GLOBAL:CURRENT_DB_INSTANCE_ID]] \\
  --db-snapshot-identifier [[GLOBAL:SNAPSHOT_NAME]] \\
  --region [[GLOBAL:AWS_REGION]]`
  }
  language="bash"
/>

Wait for the snapshot to become available:

<InterpolatedCodeBlock
  code={
`aws rds wait db-snapshot-available \\
  --db-snapshot-identifier [[GLOBAL:SNAPSHOT_NAME]] \\
  --region [[GLOBAL:AWS_REGION]]`
  }
  language="bash"
/>

## Step 2 - Restore the snapshot to a new RDS instance

Restore the snapshot to a new instance in the database VPC. Use the same DB
subnet group and security group from the original setup. Include
`--enable-iam-database-authentication` if the new instance should use IAM auth.

<InterpolatedCodeBlock
  code={
`aws rds restore-db-instance-from-db-snapshot \\
  --db-instance-identifier [[GLOBAL:NEW_DB_INSTANCE_ID]] \\
  --db-snapshot-identifier [[GLOBAL:SNAPSHOT_NAME]] \\
  --db-instance-class db.t3.medium \\
  --db-subnet-group-name ha-platform-db-subnet \\
  --vpc-security-group-ids [[GLOBAL:DB_SG_ID]] \\
  --no-publicly-accessible \\
  --enable-iam-database-authentication \\
  --region [[GLOBAL:AWS_REGION]]`
  }
  language="bash"
/>

Wait for the new instance to become available:

<InterpolatedCodeBlock
  code={
`aws rds wait db-instance-available \\
  --db-instance-identifier [[GLOBAL:NEW_DB_INSTANCE_ID]] \\
  --region [[GLOBAL:AWS_REGION]]`
  }
  language="bash"
/>

Note the new endpoint:

<InterpolatedCodeBlock
  code={
`aws rds describe-db-instances \\
  --db-instance-identifier [[GLOBAL:NEW_DB_INSTANCE_ID]] \\
  --query 'DBInstances[0].Endpoint.Address' \\
  --output text \\
  --region [[GLOBAL:AWS_REGION]]`
  }
  language="bash"
/>

If using IAM authentication, note the `DbiResourceId` of the new instance and
update the `RDSIAMAuthKine` IAM policy to include it. Without this, platform
pods fail with `Access denied for user 'kine'` errors because the IAM
`rds-db:connect` permission is scoped to a specific RDS instance resource ID.

<InterpolatedCodeBlock
  code={
`aws rds describe-db-instances \\
  --db-instance-identifier [[GLOBAL:NEW_DB_INSTANCE_ID]] \\
  --query 'DBInstances[0].DbiResourceId' \\
  --output text \\
  --region [[GLOBAL:AWS_REGION]]`
  }
  language="bash"
/>

Add the new resource ID to the policy's `Resource` array:

<InterpolatedCodeBlock
  code={
`aws iam create-policy-version \\
  --policy-arn arn:aws:iam::[[GLOBAL:ACCOUNT_ID]]:policy/RDSIAMAuthKine \\
  --set-as-default \\
  --policy-document '{
    "Version": "2012-10-17",
    "Statement": [
      {
        "Effect": "Allow",
        "Action": "rds-db:connect",
        "Resource": [
          "arn:aws:rds-db:[[GLOBAL:AWS_REGION]]:[[GLOBAL:ACCOUNT_ID]]:dbuser:[[VAR:OLD_DBI_RESOURCE_ID:db-OLDXXXXXXXXXXXXXXXXXXXXXXXXXX]]/kine",
          "arn:aws:rds-db:[[GLOBAL:AWS_REGION]]:[[GLOBAL:ACCOUNT_ID]]:dbuser:[[VAR:NEW_DBI_RESOURCE_ID:db-NEWXXXXXXXXXXXXXXXXXXXXXXXXXX]]/kine"
        ]
      }
    ]
  }'`
  }
  language="bash"
/>

## Step 3 - Scale down the platform

Scale the deployment to zero to stop all writes to the old database.

<InterpolatedCodeBlock
  code={
`kubectl --context [[GLOBAL:CLUSTER_CONTEXT]] \\
  scale deployment -n vcluster-platform loft --replicas=0`
  }
  language="bash"
/>

Wait for all pods to stop:

<InterpolatedCodeBlock
  code={
`kubectl --context [[GLOBAL:CLUSTER_CONTEXT]] \\
  get pods -n vcluster-platform -l app=loft --watch`
  }
  language="bash"
/>

## Step 4 - Update the values file

Update the `dataSource` in the values file to point to the new RDS endpoint:

<InterpolatedCodeBlock
  code={"config:\n  database:\n    dataSource: \"mysql://kine@tcp([[VAR:NEW_DATABASE_URL:mariadb-ha-platform-restored.xxxxxxxxxxxx.us-east-1.rds.amazonaws.com]]:3306)/kine\""}
  language="yaml"
/>

## Step 5 - Upgrade the platform

Apply the updated values file.

<Tabs
  defaultValue="cli"
  values={[
    { label: "vCluster CLI", value: "cli" },
    { label: "Helm", value: "helm" },
  ]}
>
  <TabItem value="cli">

<InterpolatedCodeBlock
  code={
`vcluster platform start \\
  --namespace vcluster-platform \\
  --kube-context [[GLOBAL:CLUSTER_CONTEXT]] \\
  --values platform-ha-values.yaml \\
  --upgrade \\
  --no-tunnel`
  }
  language="bash"
/>

  </TabItem>
  <TabItem value="helm">

<InterpolatedCodeBlock
  code={
`helm upgrade loft vcluster-platform --install --create-namespace --repository-config='' \\
  --namespace vcluster-platform \\
  --repo "https://charts.loft.sh/" \\
  --version [[VAR:CHART_VERSION:4.8.0]] \\
  --kube-context [[GLOBAL:CLUSTER_CONTEXT]] \\
  -f platform-ha-values.yaml \\
  --server-side=true --force-conflicts`
  }
  language="bash"
/>

  </TabItem>
</Tabs>

## Step 6 - Scale up the platform

The Helm upgrade keeps replicas at zero because it does not override the manual
scale-down. Scale the deployment back up.

<InterpolatedCodeBlock
  code={
`kubectl --context [[GLOBAL:CLUSTER_CONTEXT]] \\
  scale deployment -n vcluster-platform loft --replicas=3`
  }
  language="bash"
/>

Wait for all pods to become ready:

<InterpolatedCodeBlock
  code={
`kubectl --context [[GLOBAL:CLUSTER_CONTEXT]] \\
  rollout status deployment/loft -n vcluster-platform`
  }
  language="bash"
/>

## Step 7 - Verify the restore

Confirm the platform is healthy:

<InterpolatedCodeBlock
  code={
`kubectl --context [[GLOBAL:CLUSTER_CONTEXT]] \\
  get pods -n vcluster-platform -l app=loft`
  }
  language="bash"
/>

Verify the platform UI is accessible through the domain and that all replicas are running.
