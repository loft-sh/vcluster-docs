Run the command:

```yaml title="Start the platform"
vcluster platform start --host=vcluster-platform.mydomain.tld
```

:::tip Update the Domain.
Make sure you update the hostname in the command.
:::
