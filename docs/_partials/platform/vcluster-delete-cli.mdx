```bash
vcluster delete [vcluster-name]
```

:::tip Kube-Context
Deleting clusters with vCluster CLI has the advantage that vCluster CLI will also delete the kube-context for this cluster from your local kube-config file to keep everything cleaned up.
:::
