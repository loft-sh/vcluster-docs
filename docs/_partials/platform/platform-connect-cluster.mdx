    **Connect to the control plane cluster**: You can only connect to control plane clusters that have already been added to the platform.

      ```bash
      $CLUSTER_ID=<ID of the control plane cluster>
      vcluster platform connect cluster $CLUSTER_ID
      ```

<p></p>

:::info
When connecting to a control plane cluster, the kube-context automatically gets updated to the control plane cluster.
:::