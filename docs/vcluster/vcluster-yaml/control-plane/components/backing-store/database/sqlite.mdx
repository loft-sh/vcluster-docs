---
title: Embedded SQLite
sidebar_label: embedded
sidebar_position: 1
description: Configure SQLite as the virtual cluster's backing store.
---

import ConfigReference from '@site/docs/_partials/config/controlPlane/backingStore/database/embedded.mdx'

By default vCluster uses embedded [SQLite](https://www.sqlite.org/index.html) with a PersistentVolume resource to store data.

```yaml
controlPlane:
  backingStore:
    database:
      embedded:
        enabled: true
        dataSource: CONNECTION_STRING
```

Replace _`CONNECTION_STRING`_ with the connection string for your database.

You can [turn off PersistentVolume sync](vcluster/vcluster-yaml/sync/to-host/storage/persistent-volumes.mdx) for testing, where losing data on restarts is acceptable. 

## Config reference

<ConfigReference/>
