---
title: Secrets
sidebar_label: secrets
sidebar_position: 2
description: Configuration for ...
---

import SyncAllResource from '@site/docs/_partials/config/sync/toHost/secrets.mdx'

vCluster only knows about a couple of resources that actually use secrets / configmaps and tries to sync only those into the host cluster. You can enable syncing all secrets and configmaps to avoid the problem that needed secrets / configmaps are not synced to the host cluster. 

## Config reference

<SyncAllResource/>