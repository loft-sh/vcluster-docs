```bash
vcluster pro delete [vcluster-name]
```

:::tip Kube-Context
Deleting virtual clusters with vCluster.Pro CLI has the advantage that vCluster.Pro CLI will also delete the kube-context for this virtual cluster from your local kube-config file to keep everything cleaned up.
:::
