Set the `$VERSION` variable to the vCluster Platform version you want to upgrade to OR set it to the current version using:
```bash
CHART=$(kubectl get service loft -n vcluster-pro -o jsonpath={.metadata.labels.chart})
VERSION=${CHART:5}
```