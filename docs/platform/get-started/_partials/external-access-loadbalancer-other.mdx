```yaml
apiVersion: v1
kind: Service
metadata:
  name: vcluster-pro-loadbalancer
  namespace: vcluster-pro
spec:
  type: LoadBalancer
  ports:
    - name: https
      port: 443
      targetPort: 10443
      protocol: TCP
  selector:
    app: loft
```
