```bash
vcluster pro import [vcluster-name] \
  --cluster [cluster-name] \
  --namespace [vcluster-namespace] \
  --project [project-name]
```

:::tip List Legacy Virtual Clusters
You can list legacy virtual clusters via the cli by running the `vcluster pro list
--show-legacy` command.
:::
