Run this command using vCluster Platform CLI to get a list of all virtual clusters you have access to across all clusters:

```bash
vcluster pro list
```

:::tip Kube-Context
If you want to retrieve a kube-context for any of your virtual clusters, run:

```bash
vcluster pro use [vcluster-name]
```

:::
