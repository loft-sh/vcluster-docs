vCluster.Pro uses the CNCF project dex for single sign-on.

The easiest case is this one:

- `$VCLUSTER_PRO_HOSTNAME = vcluster-pro.mycompany.tld` (where vCluster.Pro is running)
- `$DEX_HOSTNAME = dex.mycompany.tld` (where dex should be running)
