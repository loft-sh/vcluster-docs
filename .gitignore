node_modules
.vscode/
.DS_Store
.docusaurus/
.idea/
build/
audits/
justfile
# Local Netlify folder
.netlify
# Release-dispatch receiver checks out upstream source under source-*/
# (e.g. source-vcluster/) for the generators to consume. These are
# transient working dirs, never docs content; keep them out of any
# auto-generated docs-sync PR.
source-*/
# Keep Claude Code config tracked (overrides global gitignore)
!CLAUDE.md
!.claude
.claude/worktrees/
.playwright-mcp
slack.md
TEST_QUICKSTART_PLAN.md
# Runtime artifact written by scripts/check-eos-eol-labels.js on drift
eos-eol-drift-report.md
.user/
# Drift tool build artifacts (built fresh by hack/release/run-drift-check.sh)
hack/cli-drift/cli-drift
hack/cli-drift/bin
hack/config-drift/config-drift
hack/config-drift/bin
