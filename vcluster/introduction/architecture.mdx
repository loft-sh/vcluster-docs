---
title: Architecture
sidebar_label: Architecture
sidebar_position: 2
sidebar_class_name: host-nodes private-nodes standalone
---

import HowDoesSyncingWork from '../_fragments/how-does-syncing-work.mdx'
import TenancySupport from '../_fragments/tenancy-support.mdx';
import GlossaryTerm from '@site/src/components/GlossaryTerm';
import ControlPlanesAsPods from '@site/static/media/diagrams/control-planes-as-pods.svg';
import ControlPlanesOnVMs from '@site/static/media/diagrams/control-planes-on-vms.svg';

In most deployment models, vCluster provisions <GlossaryTerm term="tenant-cluster">tenant clusters</GlossaryTerm> on top of existing infrastructure. Each tenant cluster runs a dedicated <GlossaryTerm term="control-plane">control plane</GlossaryTerm> on a <GlossaryTerm term="control-plane-cluster">Control Plane Cluster</GlossaryTerm> you operate. The control plane is a containerized process with its own <GlossaryTerm term="api-server">API server</GlossaryTerm>, controller manager, and data store. How you deploy worker nodes determines the tenancy model and the isolation level each tenant receives. In [Standalone mode](#vcluster-standalone), no pre-existing Kubernetes cluster is required.

## Tenancy models

vCluster supports the full isolation spectrum. The tenancy model is set at creation time. It determines whether tenants share compute and infrastructure, or they run on fully dedicated nodes.

### Private Nodes

Private Nodes provide the strongest isolation available in vCluster. Each tenant cluster runs on its own dedicated physical nodes, with a separate CNI, CSI, and control plane. No compute, network, or storage is shared between tenants. From the tenant's perspective, the environment is indistinguishable from a single-tenant Kubernetes cluster.

This model suits GPU tenants, regulated workloads, and compliance-sensitive environments where full infrastructure separation is required.

<figure>
<img
  src={require('@site/static/media/diagrams/private-nodes.png').default}
  alt="Architecture for Private Nodes"
 />
  <figcaption>Architecture for Private Nodes</figcaption>
</figure>

### Dedicated Nodes

Dedicated Nodes give each tenant cluster exclusive access to a set of physical nodes, without provisioning separate clusters. Kubernetes node selectors scope each tenant's workloads to a labeled node pool, ensuring compute separation without duplicating platform infrastructure.

This model suits teams that need predictable per-tenant performance while sharing the Control Plane Cluster and platform services.

<figure>
<img
  src={require('@site/static/media/diagrams/dedicated-nodes.png').default}
  alt="Architecture for Dedicated Nodes"
 />
  <figcaption>Architecture for Dedicated Nodes</figcaption>
</figure>

### Virtual Nodes

Virtual Nodes provide node-level isolation semantics over shared compute. vNode virtualizes node boundaries within a shared cluster. Each tenant cluster gets its own view of the node environment, with taints, tolerations, and scheduling boundaries enforced per tenant. No dedicated physical nodes are required.

This model suits GPU scheduling scenarios where dedicated hardware is impractical but node-level isolation semantics are required.

<figure>
<img
  src={require('@site/static/media/diagrams/virtual-nodes.png').default}
  alt="Architecture for Virtual Nodes"
 />
  <figcaption>Architecture for Virtual Nodes</figcaption>
</figure>

### Shared Nodes

Shared Nodes run workloads from multiple tenant clusters on the same physical Kubernetes nodes. Each tenant cluster has its own isolated control plane, API server, and CRDs, but workloads are scheduled without node-level isolation.

This model maximizes resource utilization. It suits internal developer environments, CI/CD pipelines, and high-density workloads where compute separation is not required.

<figure>
<img
  src={require('@site/static/media/diagrams/shared-nodes.png').default}
  alt="Architecture for Shared Nodes"
 />
  <figcaption>Architecture for Shared Nodes</figcaption>
</figure>

<!-- vale off -->
### vCluster Standalone
<!-- vale on -->

vCluster Standalone runs without a Control Plane Cluster. The tenant cluster runs as a self-contained process, typically inside a single container or VM. It bootstraps its own control plane without relying on existing cluster infrastructure.

This model suits single-tenant deployments, edge environments, local development, and air-gapped scenarios where a pre-existing Kubernetes cluster is not available.

<figure>
  <ControlPlanesOnVMs style={{width: '100%', height: 'auto'}} role="img" aria-label="Two vCluster Standalone control planes running as binaries on dedicated VMs, each connected to external worker nodes" />
  <figcaption>vCluster Standalone control planes as binaries on dedicated VMs</figcaption>
</figure>

## Control plane architecture

<TenancySupport hostNodes="true" />

### Virtual control plane {#virtual-control-plane}

<figure>
  <ControlPlanesAsPods style={{width: '100%', height: 'auto'}} role="img" aria-label="Multiple tenant cluster control planes running as pods on a Control Plane Cluster, each connected to external worker nodes" />
  <figcaption>Tenant cluster control planes as pods on a Control Plane Cluster</figcaption>
</figure>

Each tenant cluster runs a dedicated virtual control plane as a container on the Control Plane Cluster. This control plane orchestrates all operations within the tenant cluster and manages interaction with the underlying infrastructure.

The virtual control plane is deployed as a single pod, managed as a StatefulSet (default) or Deployment, containing the following components:

- A **Kubernetes API server**, which is the management interface for all API requests within the tenant cluster.
- A **controller manager**, which maintains the state of Kubernetes resources like pods, ensuring they match the desired configuration.
- A **data store**, which stores all API resources. By default, an embedded SQLite database is used, but you can [choose other data stores](../configure/vcluster-yaml/control-plane/components/backing-store/) like <GlossaryTerm term="etcd">etcd</GlossaryTerm>, MySQL, and PostgreSQL.
- A **syncer**, which synchronizes resources between the tenant cluster and Control Plane Cluster and facilitates workload management on the underlying infrastructure.
- A **scheduler**, which is an optional component for scheduling workloads. By default, vCluster reuses the Control Plane Cluster's scheduler to reduce resource usage. You can [enable the virtual scheduler](../configure/vcluster-yaml/control-plane/other/advanced/virtual-scheduler) when you need node labels, taints, drain operations, or custom scheduling behavior.

### Syncer

<figure>
<img
  src={require('@site/static/media/rebranding/vcluster-architecture.png').default}
  alt="Syncer Architecture"
 />
  <figcaption>vCluster Architecture for the syncer</figcaption>
</figure>

When a tenant cluster has no dedicated worker nodes, the <GlossaryTerm term="syncer">syncer</GlossaryTerm> translates tenant cluster Pod resources into pods on the Control Plane Cluster. The Control Plane Cluster schedules those pods, and the syncer keeps both in sync.

Higher-level resources like Deployments, StatefulSets, and CRDs exist only inside the tenant cluster. They never reach the Control Plane Cluster's API server or data store.

By default, the syncer propagates low-level resources like ConfigMaps and Secrets to the host namespace. The Control Plane Cluster scheduler needs these to schedule pods.

The syncer also propagates changes from the Control Plane Cluster back into the tenant cluster, keeping it current with the underlying infrastructure. You can [read more about how the syncer works](../configure/vcluster-yaml/sync/) and configure which resources are synced for your use case.

### Tenant namespace

Each tenant cluster runs as a StatefulSet (default) or Deployment inside a namespace on the Control Plane Cluster. Resources exist either within the tenant cluster itself or within that namespace on the Control Plane Cluster.

A Control Plane Cluster namespace contains exactly one tenant cluster. Nesting tenant clusters inside one another is also supported.

<!-- vale off -->

### Networking

Networking within tenant clusters covers communication inside the tenant cluster, between tenant clusters, and between tenant clusters and the Control Plane Cluster.

#### Ingress traffic

Instead of running a separate Ingress controller in each tenant cluster, vCluster can [synchronize Ingress resources](../configure/vcluster-yaml/sync/to-host/networking/ingresses) to use the Control Plane Cluster's Ingress controller. This reduces per-tenant overhead and simplifies DNS management across tenant clusters.

#### DNS

By default, each tenant cluster deploys its own [CoreDNS](../configure/vcluster-yaml/control-plane/components/coredns) instance. CoreDNS lets pods within the tenant cluster resolve other services in the same environment. The syncer maps service DNS names in the tenant cluster to their corresponding IP addresses on the Control Plane Cluster, following Kubernetes DNS naming conventions.

With vCluster Pro, [CoreDNS can be embedded](../configure/vcluster-yaml/control-plane/components/coredns) directly into the control plane pod, reducing the per-tenant footprint.

#### Communication within a tenant cluster

* **Pod to pod** — Pods within a tenant cluster communicate using the Control Plane Cluster's network infrastructure. No additional configuration is required. The syncer ensures Kubernetes-standard networking is maintained.

* **Pod to service** — Services within a tenant cluster are synchronized to allow pod communication, using [CoreDNS](../configure/vcluster-yaml/control-plane/components/coredns) for domain name resolution.

* **Pod to Control Plane Cluster service** — Services from the Control Plane Cluster can be [replicated](../configure/vcluster-yaml/networking/replicate-services) into a tenant cluster, allowing tenant pods to access platform services by name.

* **Pod to another tenant cluster's service** — Services can be mapped [between tenant clusters](../configure/vcluster-yaml/networking/resolve-dns.mdx) using DNS configuration that routes requests from one tenant cluster to another.

#### Communication from the Control Plane Cluster

* **Control Plane Cluster pod to tenant cluster service** — Control Plane Cluster pods can access services within a tenant cluster by [replicating](../configure/vcluster-yaml/networking/replicate-services#virtual-cluster-to-host-cluster) tenant cluster services to any Control Plane Cluster namespace.
