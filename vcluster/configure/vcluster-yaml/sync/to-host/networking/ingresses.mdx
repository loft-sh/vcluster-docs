---
title: Ingresses
sidebar_label: ingresses
sidebar_position: 1
sidebar_class_name: host-nodes
description: Configuration for syncing ingresses to host
---

import Ingress from '../../../../../_partials/config/sync/toHost/ingresses.mdx'
import TenancySupport from '../../../../../_fragments/tenancy-support.mdx';

<TenancySupport hostNodes="true" />

{/*
`ingresses.enabled`:
- covered by the generic section here: https://www.vcluster.com/docs/syncer/config#enable-or-disable-synced-resources
- new ingress version support added with https://github.com/loft-sh/vcluster/pull/104, but not really worth mentioning in documentation
*/}

By default, this is disabled.

When enabled, sync all [Ingress](https://kubernetes.io/docs/concepts/services-networking/ingress/) resources from the tenant cluster to the control plane cluster. Use this option to make a tenant cluster service available using a hostname/domain without having to configure DNS for each tenant cluster. This requires a compatible Ingress controller running on the control plane cluster. A single shared controller can reconcile Ingress resources synced from multiple tenant clusters. vCluster preserves a tenant's `spec.ingressClassName` unchanged. A dedicated per-tenant IngressClass alone doesn't isolate tenants, since a tenant can still name another tenant's class. A namespace-scoped controller instance per tenant closes this regardless of which class a tenant names. [`sync.fromHost.ingressClasses`](../../from-host/ingress-classes.mdx) with a selector only helps partially. It skips the check entirely when `spec.ingressClassName` is empty, or when a tenant uses the legacy `kubernetes.io/ingress.class` annotation instead. Pair it with control plane cluster admission that also constrains those two paths and any classless Ingress that would fall through to a default class.

:::warning Hostname claims aren't scoped to a tenant
vCluster syncs a tenant's `spec.rules[].host` unchanged. It doesn't rewrite or scope the hostname to that tenant. If tenant clusters share a control plane cluster ingress controller, a tenant can claim a hostname belonging to another tenant or to platform infrastructure. The controller's own conflict-resolution behavior decides which rule wins, not vCluster. See [Ingress and Gateway API hostname claims](../../../../../security/shared-nodes-hardening.mdx#ingress-and-gateway-api-hostname-claims) in the shared node hardening guide.
:::

:::tip Prefer Gateway API for new workloads
For tenant traffic served by a Gateway controller on the control plane cluster, [Gateway API sync](./gateway-api.mdx) is the modern alternative. It syncs `Gateway`, `HTTPRoute`, `TLSRoute`, `ReferenceGrant`, and `BackendTLSPolicy`, and lets tenants import host `GatewayClass` resources via [`sync.fromHost.gatewayClasses`](../../from-host/gateway-classes.mdx). Ingress sync remains supported for workloads that still depend on `networking.k8s.io/v1` Ingress resources.
:::

When enabled, vCluster automatically tries to detect the supported ingress version (`networking.k8s.io/v1` or `networking.k8s.io/v1beta1`).

### Sync Ingresses from the tenant to control plane cluster

```YAML
sync:
  toHost:
    ingresses:
      enabled: true
```

## Patches

Use `sync.toHost.ingresses.patches` to transform Ingress fields, such as `spec.rules[*].host`, while syncing to the control plane cluster. See [Patching synced resources](../../patching/README.mdx) for syntax, directionality, and examples.

## Config reference

<Ingress/>
