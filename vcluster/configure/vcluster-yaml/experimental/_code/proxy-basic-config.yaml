# Basic Resource Proxy configuration
# Proxies Workflow resources from alpha.sh/v1 to a target virtual cluster
experimental:
  proxy:
    customResources:
      workflows.alpha.sh/v1:
        enabled: true
        targetVirtualCluster:
          name: "target"
