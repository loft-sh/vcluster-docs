---
title: Edge Distribution
sidebar_label: Edge Distribution
sidebar_position: 8
description: Deploy tenant workloads at distributed edge sites managed from a central control plane. Day 0, Day 1, and Day 2 path for telcos and edge compute providers.
---

Deploy tenant workloads at distributed edge sites without managing a separate cluster at each one. Telecommunications providers, ISPs, and edge compute operators use this model to run low-latency services at POPs, exchange points, and remote facilities from a single central control plane.

**Typical stack:** Central Standalone (HA) as the Control Plane Cluster. Central vCluster Platform. Worker nodes at each edge site joined to the central control plane via VPN. Tenant control planes run centrally; pods schedule onto edge nodes.

<figure>
  <img
    style={{ display: "block", margin: "0 auto", maxWidth: "100%" }}
    src="/docs/img/production-paths/edge-distribution.png"
    alt="Edge distribution architecture: central control plane with tenant clusters scheduling pods onto distributed edge site nodes"
  />
  <figcaption>Central control plane managing tenant workloads across distributed edge sites</figcaption>
</figure>

**What makes this path different:** The compute is distributed but the control plane is not. Your ops team manages the full fleet from one place. Tenants get a standard Kubernetes experience regardless of which edge site their workload lands on.

## Day 0: Design decisions

| Decision | Read next | Outcome |
| --- | --- | --- |
| Design the central-edge topology | [Standalone deployment](../deploy/control-plane/binary/basics.mdx), [VPN](../deploy/worker-nodes/private-nodes/vpn.mdx) | Central Standalone hosts all tenant control planes. Edge sites contribute worker nodes over VPN. |
| Define edge site requirements | [Node requirements](../deploy/worker-nodes/private-nodes/node-requirements.mdx) | Document the OS, networking, and connectivity requirements for each edge site before joining nodes. |
| Plan node labeling and scheduling | [Private Nodes](../deploy/worker-nodes/private-nodes/overview.mdx) | Label nodes by site, region, or tier so tenant workloads can be scheduled to the correct edge location. |
| Plan tenant isolation at edge | [vNode docs](https://www.vnode.com/docs/) | Decide whether edge workloads need runtime isolation, especially for multi-tenant edge sites. |

## Day 1: Stand up the first edge site

1. Deploy [vCluster Standalone](../deploy/control-plane/binary/basics.mdx) centrally and move to [HA](../deploy/control-plane/binary/high-availability.mdx).
2. Install [vCluster Platform](/platform/install/quick-start-guide) and configure [SSO](/platform/configure/platform-configs/single-sign-on) and [permissions](/platform/administer/users-permissions/overview).
3. Establish [VPN connectivity](../deploy/worker-nodes/private-nodes/vpn.mdx) to the first edge site.
4. Join edge site nodes to the central Control Plane Cluster. Apply site labels.
5. Create a [template](/platform/administer/templates/overview) that schedules tenant workloads to the correct edge site via node selectors or affinity rules.
6. Provision a test tenant cluster. Validate that pods land on the intended edge nodes.
7. Document the site onboarding playbook. Repeat steps 3-6 for each additional edge site.

## Day 2: Operate

| Operation | Read next |
| --- | --- |
| Monitor edge nodes and tenant workloads | [Fleet monitoring](/platform/maintenance/monitoring/fleet-monitoring-otel) |
| Add and remove edge sites | Site onboarding playbook (internal), [VPN](../deploy/worker-nodes/private-nodes/vpn.mdx) |
| Upgrade tenant clusters and Platform | [Upgrade vCluster](../manage/upgrade/upgrade-version.mdx), [upgrade Platform](/platform/maintenance/upgrade-migrate/upgrade) |
| Handle site connectivity failures | [Platform HA](/platform/maintenance/reliability-scaling/high-availability), [multi-region Platform](/platform/administer/clusters/advanced/multi-region) |
