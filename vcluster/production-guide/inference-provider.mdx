---
title: "Inference Provider: Managed Model Serving"
sidebar_label: Inference Provider
sidebar_position: 2
description: Build a managed inference platform on GPU infrastructure. Day 0, Day 1, and Day 2 path for inference providers.
---

Build managed inference endpoints on your GPU infrastructure. Customers interact with your product API and model endpoints. Platform, vCluster, vMetal, and vNode form the operations layer your team runs behind it.

**Typical stack:** Platform as the management plane. Standalone (HA) or Kubernetes-hosted control planes for tenant clusters. Private GPU nodes for dedicated inference customers. vMetal for bare metal GPU lifecycle. vNode for runtime isolation when customers run custom containers, adapters, plugins, or other untrusted code.

<figure>
  <img
    style={{ display: "block", margin: "0 auto", maxWidth: "100%" }}
    src="/docs/img/production-paths/multi-tenant.png"
    alt="Inference provider architecture: central control plane that creates isolated tenant clusters backed by dedicated GPU nodes"
  />
  <figcaption>Central control plane managing isolated inference environments backed by GPU capacity</figcaption>
</figure>

**What makes this path different:** Customers don't use Platform directly. They call your product API or inference endpoint. Your product uses Platform and vCluster APIs to create tenant clusters, attach GPU capacity, apply templates, enforce quotas, publish routes, and retire capacity when endpoints are no longer needed.

## Day 0: Design decisions

| Decision | Read next | Outcome |
| --- | --- | --- |
| Choose the customer-facing product boundary | [Platform API](/platform/api/use-api), [Projects](/platform/administer/projects/create) | Decide which actions your product exposes, such as endpoint creation, model selection, GPU tier selection, quota changes, endpoint updates, and deletion. Customers shouldn't need Platform access. |
| Choose the inference tenancy model | [Choose an inference tenancy model](#choose-an-inference-tenancy-model), [Architecture](../introduction/architecture.mdx) | Decide whether each customer, model family, or endpoint tier gets a shared serving pool, a tenant cluster, private GPU nodes, or vNode runtime isolation. |
| Plan GPU capacity classes | [vMetal GPU fleet operations](https://www.vmetal.ai/docs/operate/gpu-fleet), [GPU presentation modes](https://www.vmetal.ai/docs/plan/gpu-presentation), [GPU and accelerators](../introduction/gpu-and-accelerator-support.mdx) | Define node types by GPU model, GPU count, region, rack, and reservation model. Keep MIG, vGPU, and Dynamic Resource Allocation decisions in the vendor stack. |
| Define serving runtime templates | [Model serving runtimes](../integrations/model-serving.mdx), [Templates](/platform/administer/templates/overview), [Certified Stacks](/platform/integrations/certified-stacks) | Standardize the components each endpoint receives, such as GPU Operator, scheduler, model server, ingress or Gateway API routes, metrics, and secrets. |
| Plan endpoint routing | [Model serving runtimes](../integrations/model-serving.mdx#expose-an-inference-endpoint), [Gateway API](../key-features/gateway-api.mdx) | Decide whether tenant clusters attach routes to platform-owned shared Gateways, tenant-owned Gateways, or another ingress layer controlled by your product. |
| Plan autoscaling signals | [GPU and inference autoscaling](../integrations/gpu-hpa-dcgm.mdx), [Monitoring overview](/platform/maintenance/monitoring/overview) | Combine hardware metrics, such as GPU utilization and memory, with serving metrics, such as request concurrency, queue depth, latency, tokens per second, and time to first token. |
| Plan durability and operations | [Backing store](../configure/vcluster-yaml/control-plane/components/backing-store/README.mdx), [Platform HA](/platform/maintenance/reliability-scaling/high-availability), [Platform backup](/platform/maintenance/backup-restore/backup-restore-platform) | Choose data stores, backup policy, control plane availability, and recovery procedures before customers depend on endpoints. |

### Choose an inference tenancy model

Inference providers usually need more than one tenancy model. Use the model that matches the customer's trust level, performance expectation, and isolation requirement.

| Model | Use when | Tradeoff |
| --- | --- | --- |
| Shared serving tier | Customers use provider-owned models and don't run custom containers or plugins. | Highest utilization, but weaker customer isolation. Use this for trusted workloads and low-risk endpoint tiers. |
| Tenant cluster per customer | Customers need their own Kubernetes API boundary, custom resources, secrets, and lifecycle. | Strong control plane isolation without automatically dedicating every GPU node. |
| Private GPU nodes | Customers need dedicated worker-node infrastructure, predictable performance, separate CNI/CSI, or compliance isolation. | Stronger isolation and performance predictability, with more capacity planning. |
| vNode runtime isolation | Customers run untrusted code, custom containers, privileged workloads, adapters, or dynamic execution paths on shared nodes. | Adds a workload sandbox boundary, but doesn't replace private nodes when customers require separate infrastructure. |

Start with tenant clusters for the API and lifecycle boundary. Add private GPU nodes for dedicated endpoint tiers. Add vNode when custom inference workloads need a stronger runtime boundary.

### Define the endpoint contract

Before building automation, define the customer-facing contract separately from the Platform resources that implement it.

| Product concept | Platform or tenant resource |
| --- | --- |
| Customer account, organization, or workspace | Platform project, quotas, allowed templates, and allowed node types |
| Endpoint tier, such as `h100-dedicated` or `l40s-shared` | Template parameters, node type constraints, and quota policy |
| Endpoint instance | Tenant cluster, namespace, model-serving Deployment, Service, route, secrets, and metrics resources |
| Endpoint URL and API key | Gateway API or ingress route, DNS entry, certificate, and provider authentication layer |
| Endpoint lifecycle event | Platform API call, template update, tenant workload rollout, drain, scale, or delete operation |

Keep this mapping in your product control plane. Customers should see endpoint status, model status, URL, usage, and errors. Your operators should see the backing project, tenant cluster, GPU node claim, template version, route, and runtime rollout state.

## Day 1: Stand up the first production inference endpoint

:::note
Steps 3 and 4 configure Platform for your platform engineering team and automation. Customers should provision endpoints through your product.
:::

1. Install [vCluster Platform](/platform/install/quick-start-guide). If building from bare metal, deploy [vCluster Standalone](../deploy/control-plane/binary/basics.mdx) first, then move to [Standalone HA](../deploy/control-plane/binary/high-availability.mdx) before production traffic.
2. Configure [backing store](../configure/vcluster-yaml/control-plane/components/backing-store/README.mdx), [Platform HA](/platform/maintenance/reliability-scaling/high-availability), and [Platform backup](/platform/maintenance/backup-restore/backup-restore-platform).
3. Configure [SSO](/platform/configure/platform-configs/single-sign-on), [permissions](/platform/administer/users-permissions/overview), and [access keys](/platform/api/resources/ownedaccesskey) for the internal automation that provisions endpoints.
4. Create [projects](/platform/administer/projects/create), [templates](/platform/administer/templates/overview), [quotas](/platform/administer/projects/quotas), and [allowed node types](/platform/administer/projects/allowed/node-types) for each customer tier.
5. Set up [vMetal](https://www.vmetal.ai/docs/) and the [Metal3 node provider](/platform/administer/node-providers/metal3). Register BMC credentials, define OS images, label GPU servers, and create node types for the inference tiers you sell.
6. Create a tenant cluster template for the endpoint tier. The template should enable [Private Nodes](../deploy/worker-nodes/private-nodes/overview.mdx), restrict GPU node selection, install the [GPU Operator or device plugin](../introduction/gpu-and-accelerator-support.mdx#install-the-nvidia-gpu-operator-in-a-tenant-cluster), enable any required sync settings, and deploy your scheduler and metrics stack.
7. Create a runtime template or post-create workflow for the endpoint. It should install the model-serving Deployment, Service, secrets, storage, metrics resources, and Gateway API or ingress resources. See [Model serving runtimes](../integrations/model-serving.mdx) for the tenant-runtime pattern.
8. Configure endpoint routing with Gateway API, ingress, or your product's traffic layer. Prefer Gateway API for new HTTP endpoint deployments, and decide whether the route attaches to a platform-owned imported Gateway or a tenant-owned Gateway.
9. Configure autoscaling with hardware metrics, serving metrics, or both. Use [GPU and inference autoscaling](../integrations/gpu-hpa-dcgm.mdx) for the hardware-metric baseline and serving-metric patterns such as queue depth, request concurrency, latency, and tokens per second.
10. Build the product automation path. For an endpoint create request, your product should choose the project and template version, pass tier parameters, create or reuse the tenant cluster, wait for GPU capacity, deploy the runtime template, publish the route, and return endpoint status and URL.
11. Validate the first endpoint from outside the tenant cluster. Confirm the endpoint accepts authorized traffic, rejects unauthorized traffic, reports healthy model status, emits metrics, and can be traced back to the tenant cluster, node type, template version, and route.
12. Validate isolation and quota enforcement. Confirm the customer can't see Platform internals, other tenant clusters, disallowed templates, disallowed GPU node types, or host cluster resources.
13. Test the full endpoint lifecycle through your product API: create, warm, scale, update model or adapter, rotate credentials, drain, delete, and reclaim GPU capacity.

### Endpoint provisioning flow

Use this flow as the acceptance test for the first provider-managed endpoint:

1. Customer calls your product API with model, region, endpoint tier, and scaling parameters.
2. Product API authenticates the customer and checks product-level entitlement.
3. Product automation calls Platform with an internal access key.
4. Platform creates or selects the project, tenant cluster template, runtime template, quota, and allowed node types for the customer's tier.
5. Private Nodes or Auto Nodes attach GPU capacity from the selected node type.
6. The tenant cluster installs the GPU stack, metrics stack, model-serving runtime, Service, and route.
7. Gateway API, ingress, or your traffic layer publishes the endpoint hostname.
8. Product API returns endpoint status, URL, model state, and operational identifiers.
9. Day 2 automation watches rollout, route, GPU, and serving metrics and reconciles endpoint status back to your product.

## Day 2: Operate

| Operation | Read next |
| --- | --- |
| Manage GPU capacity and machine lifecycle | [vMetal GPU fleet operations](https://www.vmetal.ai/docs/operate/gpu-fleet), [Metal3 node provider](/platform/administer/node-providers/metal3), [Manage private nodes](../deploy/worker-nodes/private-nodes/manage.mdx) |
| Expose and troubleshoot endpoint routing | [Model serving runtimes](../integrations/model-serving.mdx#expose-an-inference-endpoint), [Gateway API](../key-features/gateway-api.mdx), [Gateway API sync troubleshooting](../troubleshoot/gateway-api-sync.mdx) |
| Scale model-serving workloads | [GPU and inference autoscaling](../integrations/gpu-hpa-dcgm.mdx), [Fleet monitoring](/platform/maintenance/monitoring/fleet-monitoring-otel) |
| Roll model runtimes, drivers, and OS images | [Deploy changes](../manage/deploy-changes.mdx), [vMetal GPU fleet operations](https://www.vmetal.ai/docs/operate/gpu-fleet#rotating-os-images), [Upgrade vCluster](../manage/upgrade/upgrade-version.mdx) |
| Enforce customer capacity boundaries | [Quotas](/platform/administer/projects/quotas), [Allowed node types](/platform/administer/projects/allowed/node-types), [Projects](/platform/administer/projects/create) |
| Back up and restore tenant clusters and Platform | [Snapshots](../manage/backup-restore/backup.mdx), [restore](../manage/backup-restore/restore.mdx), [Platform backup](/platform/maintenance/backup-restore/backup-restore-platform) |
| Manage vNode compatibility during upgrades | [vNode limitations](https://www.vnode.com/docs/limitations/), [vNode configuration](https://www.vnode.com/docs/configuration/) |
