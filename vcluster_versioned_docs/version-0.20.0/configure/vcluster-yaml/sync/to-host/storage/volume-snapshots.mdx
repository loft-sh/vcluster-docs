---
title: Volume Snapshots
sidebar_label: volumeSnapshots
sidebar_position: 3
description: Configuration for ...
---

import Prereqs from '@site/vcluster/_fragments/volume-snapshots-host-prereqs.mdx'
import EnableSwitch from '@site/vcluster/_partials/config/sync/toHost/volumeSnapshots.mdx'

{/*
`volumeSnapshots.enabled`:
- covered by the generic section here: https://www.vcluster.com/docs/syncer/config#enable-or-disable-synced-resources
- Added to close issue: https://github.com/loft-sh/vcluster/issues/258
*/}

By default, this is disabled. 

Sync [VolumeSnapshot](https://kubernetes.io/docs/concepts/storage/volume-snapshots/) resources from the virtual cluster to the host cluster.

Until enabled, creating a VolumeSnapshot custom resource in the virtual cluster has no effect on the host cluster. 

### Host prerequisites

<Prereqs/>

### Sync VolumeSnapshots from the virtual to host cluster

```YAML
sync:
  toHost:
    volumeSnapshots:
      enabled: true
```
## Config reference

<EnableSwitch/>