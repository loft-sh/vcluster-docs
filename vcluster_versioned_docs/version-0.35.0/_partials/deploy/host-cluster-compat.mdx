export const HostClusterCompat = ({ distro }) => (
  <>
    The Kubernetes distro of vCluster is completely independent of the Kubernetes distro of the host cluster. This means you can run vCluster using {distro} distribution regardless of the distribution type of the host cluster.
  </>
);