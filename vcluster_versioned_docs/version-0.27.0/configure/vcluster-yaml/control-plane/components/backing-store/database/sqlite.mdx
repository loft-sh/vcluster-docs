---
title: Embedded SQLite
sidebar_label: embedded
sidebar_position: 1
description: Configure SQLite as the virtual cluster's backing store.
sidebar_class_name: host-nodes private-nodes standalone
---

import ConfigReference from '../../../../../../_partials/config/controlPlane/backingStore/database/embedded.mdx'

<!-- vale off -->

By default, vCluster uses embedded [SQLite](https://www.sqlite.org/index.html) with a PersistentVolume resource to store data.

You can [turn off PersistentVolume sync](../../../../sync/to-host/storage/persistent-volumes.mdx) for testing, where losing data on restarts is acceptable. 

## Disable SQLite

```yaml
controlPlane:
  backingStore:
    database:
      embedded:
        enabled: false
```

Disable SQLite if you want to use a different database. See [External database](./external.mdx) for configuration details.

## Config reference

<ConfigReference/>
