import { HostClusterCompat } from '../../_partials/deploy/host-cluster-compat.mdx'

### Host cluster compatibility
<HostClusterCompat distro="k8s"/>

vCluster supports specific Kubernetes versions on host clusters. For more information, see the Kubernetes compatibility matrix in the upgrade documentation.
