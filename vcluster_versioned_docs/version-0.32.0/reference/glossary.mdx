---
title: Glossary
sidebar_label: Glossary
sidebar_position: 10
description: Glossary of terms used in vCluster documentation
---

import glossaryData from '@site/src/data/glossary.yaml';

<!-- vale off -->
# vCluster glossary {#vcluster-glossary}
<!-- vale on -->

This page contains definitions for common terms used throughout the vCluster documentation.

<!-- vale off -->
{Object.entries(glossaryData)
  .filter(([key, entry]) => {
    const typeValue = entry.type;
    return Array.isArray(typeValue) ? typeValue.includes('vcluster') : typeValue === 'vcluster';
  })
  .sort((a, b) => a[1].term.localeCompare(b[1].term))
  .map(([key, entry]) => (
    <div key={key} id={key} style={{ marginBottom: '2.5rem', borderBottom: '1px solid #eee', paddingBottom: '1.5rem' }}>
      <h3>{entry.term}</h3>
      <p>{entry.definition}</p>
      
      {entry.related && (
        <p><strong>Related terms:</strong> {entry.related.map((term, index) => (
          <span key={term}>
            {index > 0 && ', '}
            <span>{glossaryData[term]?.term || term}</span>
          </span>
        ))}</p>
      )}
    </div>
  ))
}
<!-- vale on -->