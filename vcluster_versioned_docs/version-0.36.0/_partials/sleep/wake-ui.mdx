import Flow, { Step } from '@site/src/components/Flow'
import NavStep from '@site/src/components/NavStep'
import Label from '@site/src/components/Label'

<Flow id="wakeup-manual-ui">
  <Step>
    In the <NavStep>{props.name === "virtual cluster" ? "Virtual Clusters" : "Spaces"}</NavStep> view, hover over the {props.name} that you want to wake up.
  </Step>
  <Step>
    Click on the vertical ellipsis button and select <Label>Wake Up</Label> to wake up the {props.name}.
  </Step>
  <Step>
    Notice how the <Label>Status</Label> column shows that the {props.name} is now running again after a <Label>Pending</Label> state.
  </Step>
</Flow>
