---
title: Virtual Cluster Settings
sidebar_label: Virtual Cluster Settings
---

Each virtual cluster can be configured during creation of a virtual cluster or updated after the virtual cluster is created. 

Only certain settings of a virtual cluster are able to be configured after a virtual cluster is running. 