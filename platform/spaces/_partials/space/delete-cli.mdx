```bash
vcluster platform delete space [space-name]
```

:::tip Kube-Context
Deleting spaces with vCluster CLI has the advantage that vCluster CLI will also delete the kube-context for this space from your local kube-config file to keep everything cleaned up.
:::
