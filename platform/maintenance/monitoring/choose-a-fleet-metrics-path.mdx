---
title: Choose a fleet metrics path
sidebar_label: Choose a Fleet Metrics Path
sidebar_position: 2
description: Decide between Fleet Observability, Fleet monitoring with OpenTelemetry, and Aggregating Metrics based on access scoping, deployment constraints, and required tooling.
---

import ChooseFleetMetricsPathDecision from '@site/static/media/diagrams/choose-fleet-metrics-path-decision.svg';

<GlossaryTerm term="vcluster">vCluster</GlossaryTerm> Platform offers three paths to fleet-wide metrics, each solving a different
problem well. They aren't interchangeable. Each stamps a different set of identity labels,
so a dashboard or alert built for one path won't return data on another. Pick the right one
before you build anything on top of it.

## Decide with three questions

Default to [Fleet Observability](../observability/overview.mdx). Use one of the other two paths
only when it doesn't fit, either a deployment constraint it can't reach or tooling depth it
doesn't provide yet.

1. Do different tenants or customers need dashboards scoped to only their own metrics, or do you need GPU observability? Use [Fleet Observability](../observability/overview.mdx).
2. Do you need in-depth troubleshooting guides, a golden-signals PromQL query library, and ready-made Grafana dashboards out of the box? Use [Fleet monitoring with OpenTelemetry](./fleet-monitoring-otel.mdx).
3. Do you need the fastest path to a working dashboard for a single operating team with the least setup? Use [Aggregating Metrics](./aggregating-metrics.mdx).

<figure>
  <ChooseFleetMetricsPathDecision style={{width: '100%', height: 'auto'}} role="img" aria-label="Decision flow: if you need scoped per-tenant access or GPU observability, use Fleet Observability. Otherwise, if you want a documented Shared Nodes deep-dive, a golden-signal query library, and ready-made Grafana dashboards, use Fleet monitoring with OpenTelemetry. Otherwise, use Aggregating Metrics." />
  <figcaption>Decision flow for choosing a fleet metrics path</figcaption>
</figure>

There's one exception. On vCluster Standalone or embedded Kubernetes, Fleet Observability's
bundled deploy doesn't cover you. Standalone can still use a manual collector config, but
embedded Kubernetes can't run the gateway at all. Use Fleet monitoring with OpenTelemetry or
Aggregating Metrics instead.

## Compare the three paths

<!-- vale off -->
| | Fleet Observability | Fleet monitoring with OpenTelemetry | Aggregating Metrics |
|---|---|---|---|
| Identity labels | `vcluster_platform_*`, authenticated | `vcluster_*`/`cluster`, self-reported | `loft_*`, self-reported |
| Read access control | Scoped per tenant or cluster, Platform-authorized | None, any reader sees all data | None, any reader sees all data |
| GPU observability | DCGM, NVSentinel | Not covered | Not covered |
| Requires Argo CD for the bundled deploy path | Yes (manual config available for Standalone) | No | No |
| Runs on embedded Kubernetes platform deployments | No | Yes | Yes |
| Golden-signal PromQL library and ready dashboards | A handful of example queries | Full latency/traffic/errors/saturation library, two downloadable Grafana dashboards | A few example queries, one bundled dashboard |
| Setup effort | Gateway, connector, access keys, then collectors | Register and deploy per-tenancy-model collector Apps | One built-in collector App plus standalone Prometheus/Grafana |
<!-- vale on -->

## Examples

- **A provider giving different customer teams their own Grafana view of only their tenant clusters** needs Platform-enforced scoping. Use Fleet Observability.
- **An internal platform team running vCluster Standalone with no Argo CD, wanting one shared internal dashboard** can't use Fleet Observability's bundled deploy path. Use Fleet monitoring with OpenTelemetry or Aggregating Metrics. Pick based on how much Shared Nodes troubleshooting depth and dashboard tooling you want out of the box.
- **A GPU-heavy fleet that needs to catch node faults before they take down inference jobs** needs NVSentinel. Use Fleet Observability.
- **A small internal team doing a quick proof of concept before committing to a monitoring stack** doesn't need scoped access or GPU support yet. Use Aggregating Metrics, and revisit this page once real requirements show up.

## Next steps

- Answered Fleet Observability? Continue to the [Fleet Observability quickstart](../observability/quickstart.mdx).
- Answered Fleet monitoring with OpenTelemetry? Continue to [Fleet monitoring with OpenTelemetry](./fleet-monitoring-otel.mdx).
- Answered Aggregating Metrics? Continue to [Aggregating Metrics](./aggregating-metrics.mdx).
- Building dashboards or alerts that need to work across a migration between paths? See the [labels reference](../observability/labels-reference.mdx) for the authenticated label contract and how it maps to the older label schemes.
