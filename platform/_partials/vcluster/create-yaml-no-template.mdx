import Flow, { Step } from "@site/src/components/Flow";
import NavStep from "@site/src/components/NavStep";
import Button from "@site/src/components/Button";
import Label from "@site/src/components/Label";

import FragmentProjectSelect from "../../_fragments/ui-steps/project-select.mdx"
import FragmentVirtualClustertSelect from "../../_fragments/ui-steps/virtual-cluster-select.mdx"
import FragmentVirtualClustertNew from "../../_fragments/ui-steps/virtual-cluster-new.mdx"

import PartialVirtualClusterUseCLI from "./use-vcluster.mdx";
import PartialVirtualClusterCreateOptionalUI from "./create-ui-optional-fields.mdx";

<Flow id="create-vcluster-form-no-template">
  <Step>
   <FragmentProjectSelect />
  </Step>
  <Step>
    <FragmentVirtualClustertSelect />
  </Step>
  <Step>
    <FragmentVirtualClustertNew />
  </Step>
  <Step>
    In the popup, confirm that the virtual cluster template field is empty.
  </Step>
  <Step>
    <b>[Optional]</b> Select the cluster in which to create the virtual cluster.
  </Step>
  <Step>
    <b>[Optional]</b> Add a name for your virtual cluster.
  </Step>
  <Step>
    Click the <Button>Configure</Button> to continue.
  </Step>
  <Step>
    In the drawer that appears from the right, click the Show YAML at the bottom left corner of the drawer.
  </Step>
  <Step>
    Configure your virtual cluster with options in the `values` section. Read
    more about what values can be used as it is the equivalent of the
    [vcluster.yaml](/vcluster/configure/vcluster-yaml/).
  </Step>
  <Step>
    Click the <Button>Create Virtual Cluster</Button> button.
  </Step>
  <Step>
    <PartialVirtualClusterUseCLI />
  </Step>
</Flow>

