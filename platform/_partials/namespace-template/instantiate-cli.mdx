When creating a space from the vCluster CLI, you will need to provide the name of the
project in which to deploy the space, and the name of template to use:

```bash
vcluster platform create space [space-name] --project [project-name] --template [template-name]
```
