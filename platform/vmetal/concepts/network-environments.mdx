---
title: Network environments
sidebar_label: Network environments
sidebar_position: 1
sidebar_class_name: pro
description: How a NetworkEnvironment describes the network a machine joins, who it belongs to, and which driver provisions it.
---

import Flow, {Step} from '@site/src/components/Flow';
import NavStep from '@site/src/components/NavStep';
import Button from '@site/src/components/Button';
import Label from '@site/src/components/Label';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

A `NetworkEnvironment` describes the network that machines join when a driver provisions them.
It is cluster scoped, so it isn't tied to a single project or namespace.
Projects, clusters, and standalone machines share environments, and each project can [narrow which ones it allows](../../administer/projects/allowed/network-environments.mdx).
The platform doesn't create environments automatically. A platform administrator creates each one and assigns it.
Each environment names one driver in `spec.providerRef` and carries that driver's settings in `spec.properties`.
The provider is required when you create an environment, and only that provider's node claims can use it.

Drivers use environments differently.
Some provision real infrastructure from one, such as a VPC and an address range.
Others don't require one and provision nothing from it, though they can still read its properties.
See the [NICo driver guide](../drivers/bare-metal/nico.mdx#create-a-network) for a driver that requires one.

## Decide who a network belongs to

Every environment records who it is for.
An operator creates the environment and states the assignment as a label:

| Label | Value | Meaning |
| --- | --- | --- |
| `tenant.vcluster.com/exclusive-to` | a Tenant name | An operator lends this environment to that Tenant. |
| `tenant.vcluster.com/exclusive-to` | empty string | The environment is reserved for the platform. |
| `tenant.vcluster.com/owner` | a Tenant name | The platform recorded the environment as owned by a Tenant. Don't set this label yourself. |

The platform assignment is an empty value rather than a name, because the platform has no Tenant name of its own.
The label must still be present.
Its presence, not its value, is what records an assignment, so an environment with no label at all is unassigned rather than platform owned.

Don't set both labels.
An environment is either lent to a Tenant by an operator or owned by a Tenant, never both.

The API requires an assignment when you create the environment:

```text
network environment must be assigned to a tenant or the platform
```

Tenant-scoped reads hide these labels, so an absent label in that view doesn't mean the environment is unassigned.

## What you can't change later

Once an environment carries an assignment, both the assignment and the whole `spec` are immutable:

```text
tenant assignment cannot be changed or removed
spec cannot be changed after assignment to a tenant
```

{/* TODO before merge: loft-enterprise#8321 (ENGCP-1539) makes displayName, owner, and access editable after assignment. Update this list when it merges. */}
This includes the display name, `providerRef`, every driver property, and the owner and access settings.
To change any of it, create a replacement environment and move its workloads across.
Decide the assignment and the network settings before you create the environment.
Metadata stays editable, so you can still mark or unmark the environment as its provider's default.

## Create an environment

Create an environment in the UI or apply a manifest through the management API.

<Tabs>
<TabItem value="ui" label="UI" default>

<Flow>
<Step>
Go to <NavStep>Bare Metal > Networking</NavStep> and open the <NavStep>Network Environments</NavStep> tab.
</Step>
<Step>
Click <Button>Create Network Environment</Button>.
</Step>
<Step>
Enter a <Label>Display name</Label> and a <Label>Network Environment ID</Label>.
The ID is the resource name. It can be up to 63 characters, can't contain dots, and can't change later.
</Step>
<Step>
In <Label>Tenant</Label>, select **Platform Tenant** to reserve the environment for the platform, or select the Tenant to lend it to.
This field appears only when the platform has Tenants. Without it, the environment is reserved for the platform.
</Step>
<Step>
In <Label>Infra Provider</Label>, select the provider whose machines join this network.
</Step>
<Step>
Optionally, switch on <Label>Set as default</Label> to make this the provider's default environment.
Each provider can have only one default, and this switch doesn't clear an existing one. If the list already shows a **Default** chip for this provider, turn off <Label>Set as default</Label> on that environment first.
</Step>
<Step>
Under <Label>Environment Properties</Label>, set the driver's properties.
Metal3 and KubeVirt providers offer <Label>CIDR</Label>, <Label>IP Range</Label>, and <Label>DNS Servers</Label> fields. Other providers take properties as YAML.
</Step>
<Step>
Click <Button>Create Network Environment</Button>.
</Step>
</Flow>

</TabItem>
<TabItem value="yaml" label="YAML">

This manifest reserves an environment for the platform and makes it the default for its provider:

```yaml
apiVersion: management.loft.sh/v1
kind: NetworkEnvironment
metadata:
  name: platform-network
  labels:
    tenant.vcluster.com/exclusive-to: ""
  annotations:
    machines.vcluster.com/is-default-environment: "true"
spec:
  displayName: Platform network
  providerRef: my-provider
  properties: {}
```

Replace `my-provider` with your NodeProvider's name and add the driver's properties.

</TabItem>
</Tabs>

Every authenticated user can list environments, but the list only shows the ones they can access.
A Tenant's users see only environments that Tenant owns or has been assigned.

## How a machine selects a network

A NodeClaim reaches an environment in one of two ways:

- It names one explicitly, through the network environment property or `spec.environmentRef`.
- It names none, and the provider's default applies.
  Mark that default by setting the `machines.vcluster.com/is-default-environment` annotation on the environment to `"true"`.
  Only environments whose `providerRef` matches the claim's provider are candidates, and each provider can have only one default.
  Marking a second default doesn't unmark the first. While two are marked, every claim that relies on the default fails with `multiple default network environments found for node provider`.

The claim resolves environments within its project's scope.
A Tenant project only finds environments assigned to or owned by that Tenant, and a platform project finds platform environments.
When the named environment is out of scope, the claim waits rather than failing.

A project can also restrict which environments its claims use.
A claim that names an environment its project excludes fails with `network environment is not allowed in project`.
See [allowed network environments](../../administer/projects/allowed/network-environments.mdx).

Standalone control plane nodes created through Auto Nodes copy the properties of the first `privateNodes.autoNodes` entry that uses the same provider.
They land in the same environment as that entry's worker nodes. Without a matching entry, the provider's default applies.

{/* MERGE GATE: this section describes loft-enterprise#8322 (ENGCP-1557), confirmed for 4.13 but unmerged as of 2026-09-25. Don't merge until it lands, and recheck the error strings against the merged code. */}
An environment serves only its own provider's node claims.
A project is offered only the environments whose provider one of its allowed node types names.
A claim that names another provider's environment is refused when it's created:

```text
network environment "<name>" belongs to node provider "<provider>", not "<claim provider>"
```

Creating an environment without a `providerRef` fails with `network environment must reference a node provider`.
Environments created without one on an earlier release still exist, but they can't serve node claims.

## Check that an environment is ready

Every environment reports a `Ready` condition, summarizing whether its driver has provisioned and synced the infrastructure.
Wait for `Ready` before you create machines against it:

```bash
kubectl get networkenvironments.management.loft.sh <name> -o yaml
kubectl wait networkenvironments.management.loft.sh/<name> --for=condition=Ready --timeout=10m
```

## Next steps

- [Connect the NICo driver](../drivers/bare-metal/nico.mdx) and create a network for platform or Tenant workloads.
