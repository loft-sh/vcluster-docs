---
title: Overview
sidebar_label: Overview
sidebar_position: 0
description: vMetal provisions and manages physical and virtual machines through a common Machine model, whether or not they run Kubernetes.
---

vMetal provisions and manages physical and virtual machines. It boots bare metal servers over the network, installs an operating system, configures networking, and tracks which servers are claimed and which are free to reuse. The same model covers virtual machines through other drivers.

vMetal isn't a single driver, repository, or service. It spans Platform's `NodeClaim`, `NodeProvider`, and `NodeType` APIs, vCluster's node-consumption workflows, and a portfolio of provisioning drivers. vMetal documentation is part of the Platform documentation source, and it always ships and versions in lockstep with Platform.

A **Machine** represents a claim on one provisioned server, either physical or virtual. A **provisioning driver** integrates the infrastructure that supplies and manages that server. See the [Glossary](../reference/glossary.mdx) for terminology and [Machine and server concepts](./concepts/machine-lifecycle.mdx) for the lifecycle and identity model.

## Two use cases, three outcomes

vMetal serves two use cases as peers. Both use the same Machines, the same drivers, and the same pool of servers.

**Stand up and hand out hardware.** Request a Machine on its own and get a provisioned server with your SSH keys on it. Nothing installs Kubernetes. This is how you run vMetal as a metal-as-a-service layer, or as the foundation for workloads that aren't Kubernetes at all.

**Give tenant clusters dedicated capacity.** Request a Machine for a tenant cluster and the provisioned server joins it as a Kubernetes node, with no manual join step. This use case has two shapes. A server can join as a worker, or it can carry the cluster's own control plane. The second doesn't need a separate workload cluster to host the tenant control plane, but vMetal still needs the connected cluster where the driver's own components run.

That makes three things a provisioned server can become, and they differ by a field or two on the request. Everything up to the moment the operating system boots is identical, and all three share the same setup and day-2 operations.

| Outcome | What the platform appends to cloud-init | Guide |
|---|---|---|
| Independently provisioned machine | Your SSH keys only | [Independently provisioned machines](./deploy/independently-provisioned-machines.mdx) |
| Control plane machine | SSH keys and the vCluster install script | [Control plane machines](./deploy/control-plane-machines.mdx) |
| Tenant cluster node | SSH keys and a Kubernetes join command | [Tenant cluster nodes](./deploy/tenant-cluster-nodes.mdx) |

## Evaluate vMetal

Work out whether vMetal fits your hardware and your team before you set it up.

| If you want to | Go to |
|---|---|
| Check vMetal works with your servers, BMCs, and network | [Requirements](./drivers/bare-metal/metal3/requirements.mdx) |
| Run the whole provisioning flow with no hardware at all | [Try it yourself](./drivers/bare-metal/metal3/overview.mdx#try-it-yourself) |
| See what to ask a data center or colocation provider for | [Handoff checklist](./drivers/bare-metal/metal3/requirements.mdx#handoff-checklist-for-a-data-center-or-colocation-provider) |
| Confirm your control plane cluster is a supported one | [Managed Kubernetes restrictions](./drivers/bare-metal/metal3/requirements.mdx#managed-kubernetes-restrictions) |
| Understand what vMetal doesn't do | [What the Metal3 driver doesn't provide](./drivers/bare-metal/metal3/security.mdx#what-the-metal3-driver-doesnt-provide) |
| Know where the driver's current boundaries are | [Known limits](./drivers/bare-metal/metal3/requirements.mdx#known-limits) |

Trying it yourself is the fastest way in. That guide runs Metal3, a NodeProvider, and simulated BareMetalHosts with Redfish BMC endpoints on KubeVirt VMs, so you can watch a server provision end to end before any hardware arrives.

## Where to start

Once you've decided to build on vMetal, both use cases start with the same hardware setup, whichever of the three a server ends up as.

| If you want to | Go to |
|---|---|
| Provision your first bare metal server | [Get started with Metal3](./drivers/bare-metal/metal3/overview.mdx#get-started) |
| Choose an initial provisioning workflow | [Get started](./get-started.mdx) |
| Register many servers at once | [Bulk registration](./drivers/bare-metal/metal3/server-lifecycle.mdx#bulk-registration) |
| Configure a NodeProvider and node types | [Node types and node providers](./concepts/machine-types-and-nodetypes.mdx) |
| Add, remove, and inspect servers | [Operations](./drivers/bare-metal/metal3/operations.mdx) |
| Diagnose a server that won't provision | [Troubleshooting](./drivers/bare-metal/metal3/troubleshooting.mdx) |
| Secure a production Metal3 deployment | [Production and Security](./drivers/bare-metal/metal3/security.mdx) |

### Run AI workloads on it

| If you want to | Go to |
|---|---|
| Understand GPU passthrough, vGPU, and MIG boundaries | [GPU presentation modes](./concepts/gpu-presentation-modes.mdx) |
| Provision GPU servers as tenant cluster nodes | [GPU quickstart](./drivers/bare-metal/metal3/gpu-quickstart.mdx) |
| Build a GPU fleet for an inference provider | [GPU Fleet Operations](./drivers/bare-metal/metal3/gpu-fleet-operations.mdx#inference-provider-capacity) |

### Understand the model

| If you want to | Go to |
|---|---|
| Learn vMetal terminology | [Glossary](../reference/glossary.mdx) |
| Understand the Machine and server model and lifecycle | [Machine and server concepts](./concepts/machine-lifecycle.mdx) |
| See how servers join tenant clusters | [Cluster attachment](./concepts/cluster-attachment.mdx) |
| Understand network environments and integrations | [Networking](./networking/overview.mdx) |

## What the platform owns

The platform owns the NodeProvider, NodeType, and NodeClaim resources. A `NodeClaim` is the Kubernetes resource that represents one Machine request and tracks its lifecycle. Every use above creates a `NodeClaim`. They differ only in whether the claim names a tenant cluster and whether it's marked as a control plane.

Tenant clusters consume that capacity through [Private Nodes](/docs/vcluster/deploy/worker-nodes/private-nodes/), either at a count you set or maintained automatically by [Auto Nodes](/docs/vcluster/deploy/worker-nodes/private-nodes/auto-nodes/). Auto Nodes maintains private node lifecycle in either a static mode, reconciling to a configured desired node count, or a dynamic mode, adding and removing nodes based on configurable resource requirements. Auto Nodes is one way to consume vMetal capacity, not the whole of vMetal.

## Choose a provisioning driver

vMetal provisions Machines through a portfolio of drivers. Drivers differ in implementation, maturity, and available features. Check each driver's documentation before relying on a specific capability. See [Node types and node providers](./concepts/machine-types-and-nodetypes.mdx) for the current driver list.

Metal3 provisions bare metal directly without a hypervisor in the vMetal-managed path. BCM integrates existing BCM-managed infrastructure. KubeVirt provisions Kubernetes nodes as virtual machines on infrastructure you provide separately. Each driver publishes its own requirements, capabilities, and day-two operations. This section currently documents Metal3 in the most depth.

## What vMetal provides, across drivers

Regardless of which driver provisions a Machine, vMetal gives you:

- **Common provisioning API** - the same `NodeClaim`, `NodeProvider`, and node type model across drivers, so you request capacity the same way whatever sits underneath.
- **OS image lifecycle** - each driver resolves its own OS image today. A shared, project-scoped image cache across drivers is planned but not yet available.
- **Automatic node registration** - a Machine that targets a tenant cluster joins it without a manual `kubeadm` join or agent installation. A Machine requested on its own skips this step entirely.
- **Capacity tracking and reclaim** - vMetal tracks the infrastructure assigned to a Machine. During teardown, the driver either returns reusable inventory to a pool or destroys infrastructure created for the claim.
- **Resource-aware consumption** - Tenant and Project boundaries control which infrastructure resources users can see and consume. vCluster and vNode provide separate workload and runtime isolation boundaries.

Physical and virtual Machine use cases include general compute, GPU workloads, accelerator workloads, and other AI infrastructure needs. See [GPU presentation modes](./concepts/gpu-presentation-modes.mdx) for how vMetal exposes GPUs without owning the driver stack itself.

## How vMetal fits into the platform stack

- **vMetal** provides the common `NodeClaim` provisioning model across physical and virtual provisioning drivers, whether the servers become Kubernetes nodes or machines you use directly.
- **vCluster Platform** manages Tenant and Project boundaries, node providers, and infrastructure availability.
- **vCluster** provides tenant clusters that consume Machines as private nodes, and can run its own control plane on one.
- **vNode** adds runtime isolation for workloads that need privileged capabilities, such as Docker-in-Docker or `hostPID`.
