---
title: Create Virtual Cluster Template
sidebar_label: Create Virtual Cluster Template
sidebar_position: 1
---

import Flow, { Step } from "@site/src/components/Flow";
import NavStep from "@site/src/components/NavStep";
import Button from "@site/src/components/Button";
import Label from '@site/src/components/Label';

import PartialVirtualClusterCreateOptionalTemplateUI from "../../_partials/vcluster/create-ui-optional-fields-template.mdx";

Virtual cluster templates can be created directly in the UI.


    <Flow id="create-vcluster-template-ui">
      <Step>
        Select the <NavStep>Templates</NavStep> field on the
        left-most menu bar.
      </Step>
      <Step>From the Templates sub-menu, select the <NavStep>Virtual Clusters</NavStep> option.</Step>
      <Step>
        Click <Button>Add Virtual Cluster Template</Button> button.
      </Step>
      <Step>
        In the drawer that appears from the right, give your virtual cluster
        template a name by replacing the 'my-template' placeholder name, or by
        updating the manifest YAML 'metadata.name' field.
      </Step>
      <Step>
        <b>[Optional]</b> Update the template with any additional desired
        configuration options.
        <PartialVirtualClusterCreateOptionalTemplateUI />
      </Step>
      <Step>
        Click on the <Button>Create Template</Button> button at the very bottom.
      </Step>
    </Flow>
