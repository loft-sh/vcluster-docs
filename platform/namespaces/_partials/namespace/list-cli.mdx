Run this command using vCluster CLI to get a list of all spaces you have access to across all clusters:
```bash
vcluster platform list spaces
```

:::tip Kube-Context
If you want to retrieve a kube-context for any of your namespaces, run:
```bash
vcluster platform connect space [namespace-name]
```
:::
