import Flow, { Step } from '@site/src/components/Flow'
import NavStep from '@site/src/components/NavStep'
import Button from '@site/src/components/Button'
import Label from '@site/src/components/Label'

<Flow id="create-namespace-ui-no-template">
  <Step>
    Go to the <NavStep>Projects</NavStep> view using the menu on the left
  </Step>
  <Step>
    Click on Namespaces and the <Button>Create Namespace</Button> button
  </Step>
  <Step>
    In the popup make sure the namespace template field is empty and click the <Button>Select</Button> button
  </Step>
  <Step>
    Optional: Select a cluster where to create the namespace in. If no cluster is specified, the platform will automatically select one for you.
  </Step>
  <Step>
    Specify a name of the namespace at the top and optionally configure the namespace you want to create.
  </Step>
  <Step>
    Click on the <Button>Create Namespace</Button> button
  </Step>
  <Step>

Retrieve a kube-context for this namespace using vcluster CLI:
```bash
loft use space [namespace-name] --project my-project
```

  </Step>
</Flow>