
import Tabs from '@theme/Tabs'
import TabItem from '@theme/TabItem'

You can either use curl or kubectl to create a new StackTemplate.

<Tabs
    defaultValue="kubectl"
    values={[
      {label: 'kubectl', value: 'kubectl'},
      {label: 'curl', value: 'curl'},
    ]}>
  <TabItem value="kubectl">


Create a file `object.yaml` with the following contents:
```yaml
apiVersion: management.loft.sh/v1
kind: StackTemplate
metadata:
  name: example-platform
spec:
  description: Deploys a database before the API that uses it.
  displayName: Example application platform
  tasks:
  - app:
      templateRef:
        name: postgresql
    name: database
  - app:
      templateRef:
        name: example-api
    dependsOn:
    - database
    name: api
status: {}

```

Then create the StackTemplate `example-platform` with:
```bash
kubectl create -f object.yaml
```


  </TabItem>
  <TabItem value="curl">


Create a file `object.yaml` with the following contents:
```yaml
apiVersion: management.loft.sh/v1
kind: StackTemplate
metadata:
  name: example-platform
spec:
  description: Deploys a database before the API that uses it.
  displayName: Example application platform
  tasks:
  - app:
      templateRef:
        name: postgresql
    name: database
  - app:
      templateRef:
        name: example-api
    dependsOn:
    - database
    name: api
status: {}

```

Run the following curl command to create a new StackTemplate `example-platform`:
```bash
curl -s -X POST --insecure \
     "https://$LOFT_DOMAIN/kubernetes/management/apis/management.loft.sh/v1/stacktemplates" \
     --data-binary "$(cat object.yaml)" \
     -H "Content-Type: application/yaml" \
     -H "Authorization: Bearer $ACCESS_KEY"
```


  </TabItem>
</Tabs>
