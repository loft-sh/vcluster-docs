
import Tabs from '@theme/Tabs'
import TabItem from '@theme/TabItem'

You can either use curl or kubectl to create a new StackInstance.
Make sure to set the project in the `metadata.namespace` field you want to create the StackInstance in. If your project has the id `my-project`, the corresponding namespace would be `p-my-project`.

<Tabs
    defaultValue="kubectl"
    values={[
      {label: 'kubectl', value: 'kubectl'},
      {label: 'curl', value: 'curl'},
    ]}>
  <TabItem value="kubectl">


Create a file `object.yaml` with the following contents:
```yaml
apiVersion: management.loft.sh/v1
kind: StackInstance
metadata:
  name: example-platform
  namespace: p-my-project
spec:
  destination:
    virtualCluster:
      name: development
  displayName: Example application platform
  owner:
    user: admin
  prunePolicy: Retain
  templateRef:
    name: example-platform
status: {}

```

Then create the StackInstance `example-platform` in project `my-project` with:
```bash
kubectl create -f object.yaml -n p-my-project
```


  </TabItem>
  <TabItem value="curl">


Create a file `object.yaml` with the following contents:
```yaml
apiVersion: management.loft.sh/v1
kind: StackInstance
metadata:
  name: example-platform
  namespace: p-my-project
spec:
  destination:
    virtualCluster:
      name: development
  displayName: Example application platform
  owner:
    user: admin
  prunePolicy: Retain
  templateRef:
    name: example-platform
status: {}

```

Run the following curl command to create a new StackInstance `example-platform` in project `my-project`:
```bash
curl -s -X POST --insecure \
     "https://$LOFT_DOMAIN/kubernetes/management/apis/management.loft.sh/v1/namespaces/p-my-project/stackinstances" \
     --data-binary "$(cat object.yaml)" \
     -H "Content-Type: application/yaml" \
     -H "Authorization: Bearer $ACCESS_KEY"
```


  </TabItem>
</Tabs>
