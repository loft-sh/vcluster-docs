---
title: App Instance
sidebar_label: App Instance
---
import Reference from "../_partials/resources/appinstances/reference.mdx"
import Retrieve from "../_partials/resources/appinstances/retrieve.mdx"
import Create from "../_partials/resources/appinstances/create.mdx"
import Update from "../_partials/resources/appinstances/update.mdx"
import Delete from "../_partials/resources/appinstances/delete.mdx"

AppInstance deploys an App into a tenant cluster, space, or connected cluster, and reports the resulting Helm release status. It replaces the Task and HelmRelease resources removed in Platform 4.12. See [What are Apps](../../understand/what-are-apps.mdx) for the concept, [Upgrade to Platform 4.12](../../maintenance/upgrade-migrate/upgrade.mdx#upgrade-to-4-12) for migrating an existing app deployment, and [Retrieve App Instance Logs](appinstancelog.mdx) for the log subresource.

## AppInstance example

An example AppInstance:
```yaml
apiVersion: management.loft.sh/v1
kind: AppInstance
metadata:
  name: my-argo-cd
  namespace: p-my-project
spec:
  destination:
    virtualCluster:
      name: development
      target: vCluster
  displayName: ArgoCD
  owner:
    user: admin
  templateRef:
    name: my-app
status: {}

```

## AppInstance reference

<Reference />

## Retrieve: AppInstances

<Retrieve />

## Create: AppInstance

<Create />

## Update: AppInstance

<Update />

## Delete: AppInstance

<Delete />
