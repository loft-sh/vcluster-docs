---
title: Add Existing Virtual Clusters
sidebar_label: Add Existing
sidebar_position: 2
---

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";
import Flow, { Step } from "@site/src/components/Flow";
import NavStep from "@site/src/components/NavStep";
import Button from "@site/src/components/Button";
import Label from "@site/src/components/Label";


import FragmentClustersSelect from '../_fragments/ui-steps/clusters-select.mdx'

import PartialVirtualClusterImportCLI from ../../platform/_partials/vcluster/import-cli.mdx";

Virtual clusters created through the vCluster CLI can be add into any project. By adding a virtual cluster into the a project, 
a `VirtualClusterInstance` resource will automatically be created in the specified project.

Adding virtual clusters can be accomplished via the UI or the CLI.

<Tabs
  defaultValue="ui"
  values={[
    { label: "UI", value: "ui" },
    { label: "CLI", value: "cli" },
  ]}
>
  <TabItem value="ui">
    <Flow id="import-vcluster">
      <Step>
        <FragmentClustersSelect />
      </Step>
      <Step>
        Click on the cluster containing the virtual cluster that you would like to import.
      </Step>
      <Step>
        In the Cluster Management view, click the
        <Label>Virtual Clusters</Label> to view the list of virtual clusters.
      </Step>
      <Step>
        Find the virtual cluster you would like to import in the list of virtual
        clusters. Click the drop down arrow next to the virtual cluster name. In
        the drop down menu click the <Label>Import</Label> button.
      </Step>
      <Step>
        In the pop-up box, select the project you would like to import the
        virtual cluster into from the <Label>Select Project</Label> drop down
        box.
      </Step>
      <Step>
        <b>[Optional]</b> provide a name for the new 'VirtualClusterInstance'
        resource, if you do not provide a name, vCluster Platform will use the name
        of the legacy virtual cluster.
      </Step>
      <Step>
        Click the <Button>Import</Button> button.
      </Step>
    </Flow>
  </TabItem>
  <TabItem value="cli">
   <PartialVirtualClusterImportCLI />

  </TabItem>
</Tabs>
