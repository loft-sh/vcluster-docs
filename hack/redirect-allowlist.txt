# Redirect Allowlist
#
# URLs listed here are excluded from redirect validation checks.
# Each entry MUST have a comment explaining WHY it's allowlisted.
#
# Format: one URL per line. Lines starting with # are comments.

# llms.txt is auto-generated at build time by @signalwire/docusaurus-plugin-llms-txt
/docs/llms.txt

# DOC-1301: platform/administer/clusters/advanced/monitoring.mdx was deleted from the
# current (next) docs. The root URL /docs/platform/administer/clusters/advanced/monitoring
# is served by platform_versioned_docs/version-4.8.0 which still has the file. Only
# /docs/platform/next/administer/clusters/advanced/monitoring was removed and has a redirect.
/docs/platform/administer/clusters/advanced/monitoring
